import { createOfficialGraphEdge } from './OfficialGraphEdge';

export const WorkbenchEdge = createOfficialGraphEdge({
  themeId: 'plotflow-narrative-workbench',
  variant: 'workbench',
  hitAreaWidth: 36,
});
