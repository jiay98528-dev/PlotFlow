import { createOfficialGraphEdge } from './OfficialGraphEdge';

export const EngineTelemetryEdge = createOfficialGraphEdge({
  themeId: 'plotflow-engine-telemetry',
  variant: 'engine-telemetry',
  hitAreaWidth: 40,
});
