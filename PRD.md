# Fablevia（维叙）产品需求

> 文档导航：[产品与品牌索引](doc/indexes/product.md) · [总索引](doc/INDEX.md)

更新：2026-09-22。当前实现版本为 0.1.1 Preview，历史 V0.3 指开发阶段。

## 定位与用户

面向 1—5 人独立游戏团队中的开发者、叙事设计师和文案策划。用户需要清楚地编排分支剧情、维护条件和变量，并把故事交给引擎运行。

核心价值是图形化创作与可读源码共享一个故事文件。本地运行、数据可带走、不依赖联网创作。商业定价策略为 $29 买断；购买、授权和公开下载链路属于后续交付范围。

## 品牌与体验原则

中文主名为维叙、辅助名 Fablevia，英文只显示 Fablevia。品牌性格为安静、精密、有想象力。桌面应用优先长期阅读、结构理解与可恢复编辑；颜色与动效不能遮蔽保存、导出和诊断路径。主要控件键盘可达、焦点可见，状态不只依赖颜色，主题支持 reduced-motion。

## 当前产品范围

| 能力 | 需求与实现边界 |
|---|---|
| 默认创作入口 | Graph Lab。画布、章节浏览、Palette、Inspector 和 Source Drawer 组成完整图形编辑工作流 |
| 源码编辑 | Split 提供完整 .mdstory、Monaco、Outline 与分支图；与 Graph Lab 共用文件内容 |
| 节点与路线 | 创建、删除、重命名、移动节点；连接、断开、创建目标节点；编辑选项顺序和默认下一步 |
| 条件与变量 | 支持条件组、效果、类型化变量、章节作用域和引擎元信息；保留源码往返语义 |
| 数据可靠性 | 保存、重开、撤销重做、外部文件变化与会话切换不静默丢弃草稿；冲突保留用户输入并给出反馈 |
| 诊断 | 问题面板、源码标记与节点状态协作；Error 级诊断阻止错误数据导出 |
| 导出 | JSON Schema 0.2、可独立试玩 HTML、纯文本 TXT |
| 引擎 | Godot 插件和运行时；Unity C# 接口与读取示例；Unreal 数据与蓝图接口参考，不宣称三者具备同等完整工具链 |
| 离线补全 | N-gram、本地预置语料、用户语料导入和增量学习；不发送创作内容到云端 |
| 外观 | 三套内置主题：棱镜铸造台（默认）、叙事工作台、引擎遥测台 |
| 语言 | 中文默认，支持英文；中文品牌主名为维叙、辅助 Fablevia，英文只显示 Fablevia |
| 帮助与反馈 | 原生 Help 菜单打开反馈窗口；用户主动提交时通过独立 HTTPS 服务传输，邮件凭据只在服务端 |
| 平台 | 本阶段优先 Windows 桌面预览版 |

## 不可变合同

1. .mdstory 是唯一磁盘真相源，不用数据库或专有二进制保存故事。
2. GUI 修改经故事命令和文本事务写回文件；图形状态不能成为第二数据源。
3. .mdstory 系统版本保持 0.1；当前 JSON 写出为 Schema 0.2，读取兼容 0.1/0.2。
4. 引擎启动模式下，变量来自引擎，编辑器不能绕过来源约束自由创建。
5. 主题不能改变故事语义、保存、解析和导出。0.1.1 暂停全部可执行远程主题，只交付编译内置主题。

## 下一阶段候选范围

- Windows 预览版对外获取路径、官网真实产品演示与用户反馈闭环。
- 最小 Godot 项目真实运行体验与示例完善。
- 安装后首次启动引导。
- macOS/Linux 打包、自动更新、Unity 示例场景与中英文语料扩充单独排期，不作为当前 Windows 开发基线的隐含前置条件。
- 远程主题不在当前范围；未来恢复需新的声明式不可执行格式与独立签名设计。

## 维护入口

实际状态见 spec/progress.md；交互细节见 spec/design-brief-editor-ux.md；语法见 spec/syntax-formal.md；数据合同见 spec/json-schema.md；主题能力见 doc/standards-theme-development.md。这些文档分别维护自己的合同，避免重复记录测试数字和发行状态。
