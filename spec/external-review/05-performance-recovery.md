# 证据包 5：性能与恢复

- 分别打开 100、500、1000 节点故事，记录 Graph Lab 可交互、搜索定位、缩放和平移时间；搜索和视图操作后磁盘文本 hash 不得变化。
- 1000 节点搜索并居中目标，不得触发重新布局或故事修改；完成真实导出。
- 验证未保存关闭/取消、外部冲突、保存副本、空文件、仅 frontmatter、CRLF、Unicode 路径、无效扩展、ENOENT 和权限失败。
- 用系统 Edge 打开导出的 HTML，验证开始、选项和至少一次分支跳转。自动化由浏览器控制工具执行；Computer Use 只负责资源管理器启动与可见录制，不猜测 URL。

停止点：浏览器控制不可用只阻断 HTML 子项并使本包 `BLOCKED`；崩溃、数据丢失、文本被视图操作修改或可复现的错误处理缺失为 `FAIL`。
