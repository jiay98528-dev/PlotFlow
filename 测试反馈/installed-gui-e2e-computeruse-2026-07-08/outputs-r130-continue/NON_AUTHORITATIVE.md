# Historical non-authoritative output

These files are retained only for regression traceability. They predate the
small-batch candidate and the external-review schema v2, so they must not be
used to satisfy any required release or external-review case.

`blackbox-journey-SHA256SUMS.txt` records the retained files' content hashes.
